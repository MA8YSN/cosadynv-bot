# modals/

Each file here exports a default `ModalHandler` (see `types/index.ts`),
following the same pattern as `buttons/`. Used for pop-up forms — e.g. the
future Citizen ID application form, giveaway entry forms, etc.
